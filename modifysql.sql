BEGIN
	EXECUTE IMMEDIATE 'DROP TABLE RAGTEST1 PURGE';
EXCEPTION
	WHEN OTHERS THEN
		IF SQLCODE != -942 THEN
			RAISE;
		END IF;
END;
/

CREATE TABLE RAGTEST1 (
	ID                       NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
	RECORD_ID                VARCHAR2(64 CHAR) NOT NULL,
	DOC_ID                   VARCHAR2(128 CHAR) NOT NULL,
	SECTION_ID               VARCHAR2(128 CHAR) NOT NULL,
	CHUNK_ID                 VARCHAR2(128 CHAR) NOT NULL,
	CHUNK_TYPE               VARCHAR2(32 CHAR) NOT NULL,
	SOURCE_URI               VARCHAR2(2048 CHAR),
	SOURCE_TYPE              VARCHAR2(32 CHAR),
	PAGE_FROM                NUMBER,
	PAGE_TO                  NUMBER,
	TITLE                    VARCHAR2(1000 CHAR),
	BREADCRUMBS              CLOB,
	PITANJE                  CLOB,
	ODGOVOR                  CLOB,
	KONTEKST                 CLOB,
	STEPS_JSON               CLOB,
	CONSTRAINTS_JSON         CLOB,
	PREREQUISITES_JSON       CLOB,
	SYSTEM_EFFECTS_JSON      CLOB,
	BRANCHES_JSON            CLOB,
	NAVIGATION_PATHS_JSON    CLOB,
	CROSS_SYSTEM_REFS_JSON   CLOB,
	ERROR_SCENARIOS_JSON     CLOB,
	EMPHASIS_SIGNALS_JSON    CLOB,
	CUSTOM_FIELDS_JSON       CLOB,
	METADATA_JSON            CLOB,
	EMBEDDING_MODEL          VARCHAR2(256 CHAR),
	EMBEDDING_INPUT          CLOB,
	APLIKACIJA               NUMBER,
	EMBEDDING                VECTOR,
	CREATED_AT               TIMESTAMP(6) DEFAULT SYSTIMESTAMP NOT NULL,

	CONSTRAINT UQ_RAGTEST1_RECORD UNIQUE (RECORD_ID),
	CONSTRAINT UQ_RAGTEST1_CHUNK UNIQUE (CHUNK_ID),

	CONSTRAINT CK_RAGTEST1_BREADCRUMBS_JSON CHECK (BREADCRUMBS IS JSON),
	CONSTRAINT CK_RAGTEST1_STEPS_JSON CHECK (STEPS_JSON IS JSON),
	CONSTRAINT CK_RAGTEST1_CONSTRAINTS_JSON CHECK (CONSTRAINTS_JSON IS JSON),
	CONSTRAINT CK_RAGTEST1_PREREQUISITES_JSON CHECK (PREREQUISITES_JSON IS JSON),
	CONSTRAINT CK_RAGTEST1_SYSTEM_EFFECTS_JSON CHECK (SYSTEM_EFFECTS_JSON IS JSON),
	CONSTRAINT CK_RAGTEST1_BRANCHES_JSON CHECK (BRANCHES_JSON IS JSON),
	CONSTRAINT CK_RAGTEST1_NAV_PATHS_JSON CHECK (NAVIGATION_PATHS_JSON IS JSON),
	CONSTRAINT CK_RAGTEST1_CROSS_SYSTEM_REFS_JSON CHECK (CROSS_SYSTEM_REFS_JSON IS JSON),
	CONSTRAINT CK_RAGTEST1_ERROR_SCENARIOS_JSON CHECK (ERROR_SCENARIOS_JSON IS JSON),
	CONSTRAINT CK_RAGTEST1_EMPHASIS_SIGNALS_JSON CHECK (EMPHASIS_SIGNALS_JSON IS JSON),
	CONSTRAINT CK_RAGTEST1_CUSTOM_FIELDS_JSON CHECK (CUSTOM_FIELDS_JSON IS JSON),
	CONSTRAINT CK_RAGTEST1_METADATA_JSON CHECK (METADATA_JSON IS JSON)
);

CREATE INDEX IX_RAGTEST1_DOC_ID ON RAGTEST1 (DOC_ID);
CREATE INDEX IX_RAGTEST1_SECTION_ID ON RAGTEST1 (SECTION_ID);
CREATE INDEX IX_RAGTEST1_CHUNK_TYPE ON RAGTEST1 (CHUNK_TYPE);
CREATE INDEX IX_RAGTEST1_APLIKACIJA ON RAGTEST1 (APLIKACIJA);
/
